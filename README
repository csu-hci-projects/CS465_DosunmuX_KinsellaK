pip install opencv-python
pip install mediapipe
pip install SpeechRecognition
pip install pygame
pip install pyaudio

these are the libraries that are needed to make the program work.
Please make sure these are installed and there are not conflicting libraries that would cause the program to break. 
once the downloaded 
python ./main
will run the program. 
